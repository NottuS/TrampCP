<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.2//EN"
          "http://jakarta.apache.org/struts/dtds/struts-config_1_2.dtd">

<struts-config>

<!-- Form Bean Definitions -->
	<form-beans type="org.apache.struts.action.ActionFormBean">
		<form-bean name="matriculaForm" type="gov.pr.celepar.framework.form.MatriculaForm"/>
	</form-beans>

<!-- Global Exception Definitions -->
    <global-exceptions/>

<!-- Global Forward Definitions -->
    <global-forwards>
    	<forward name="error" path="/pages/ctlr_error.jsp"/>
    </global-forwards>


<!-- Action Mapping Definitions -->
    <action-mappings>
    
		<!-- Login -->
		<action path="/login" forward="loginDef"/>
		
		<!-- Entrada -->
		<action path="/entrada" forward="entradaDef"/>
		
		<!-- Erro de Acesso (Permissoes) -->
		<action path="/erro" forward="erroAcessoDef"/>

		<!-- Matricula -->
		<action path="/matricula"
            type="gov.pr.celepar.framework.action.MatriculaAction"
			name="matriculaForm"
            scope="request"
            validate="false"
            parameter="action">
			<forward name="pgEditAluno" path="editAlunoDef"/>
			<forward name="pgListAluno" path="listAlunoDef"/>
			<forward name="pgViewAluno" path="viewAlunoDef"/>        
      </action>
      
    </action-mappings>
    

<!-- Controller Configuration -->
    <controller processorClass="org.apache.struts.tiles.TilesRequestProcessor"/>


<!-- Message Resources Definitions -->
	<message-resources parameter="gov.pr.celepar.util.properties.ApplicationResources" />


<!-- Plug Ins Configuration -->

	<!-- Tiles plugin -->
    <plug-in className="org.apache.struts.tiles.TilesPlugin" >

      <!-- Path to XML definition file -->
      <set-property property="definitions-config"
                       value="/WEB-INF/tiles-defs.xml" />
    </plug-in>


	<!-- Validator plugin -->
	<plug-in className="org.apache.struts.validator.ValidatorPlugIn">
		<set-property
			property="pathnames"
			value="/WEB-INF/validator-rules.xml,/WEB-INF/validation.xml"/>
	</plug-in>

</struts-config>